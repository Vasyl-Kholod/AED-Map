import { makeStyles } from '@material-ui/core/styles';

const useAccountControlStyles = makeStyles({
  container: {
    display: 'flex',
    flexDirection: 'column',
    flex: 1
  }
});

export { useAccountControlStyles };